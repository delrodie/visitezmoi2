{% extends 'base.html.twig' %}

{% block title %}Slide{% endblock %}

{% block body %}
    <h1>Slide</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slide.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ slide.titre }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ slide.lien }}</td>
            </tr>
            <tr>
                <th>Debut</th>
                <td>{{ slide.debut }}</td>
            </tr>
            <tr>
                <th>Fin</th>
                <td>{{ slide.fin }}</td>
            </tr>
            <tr>
                <th>Media</th>
                <td>{{ slide.media }}</td>
            </tr>
            <tr>
                <th>IsValid</th>
                <td>{{ slide.isValid ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('slide_index') }}">back to list</a>

    <a href="{{ path('slide_edit', {'id': slide.id}) }}">edit</a>

    {{ include('slide/_delete_form.html.twig') }}
{% endblock %}
