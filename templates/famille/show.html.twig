{% extends 'base.html.twig' %}

{% block title %}Famille{% endblock %}

{% block body %}
    <h1>Famille</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ famille.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ famille.code }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ famille.libelle }}</td>
            </tr>
            <tr>
                <th>NombreProduit</th>
                <td>{{ famille.nombreProduit }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ famille.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('famille_index') }}">back to list</a>

    <a href="{{ path('famille_edit', {'id': famille.id}) }}">edit</a>

    {{ include('famille/_delete_form.html.twig') }}
{% endblock %}
