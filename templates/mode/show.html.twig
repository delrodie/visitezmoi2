{% extends 'base.html.twig' %}

{% block title %}Mode{% endblock %}

{% block body %}
    <h1>Mode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mode.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ mode.libelle }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ mode.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mode_index') }}">back to list</a>

    <a href="{{ path('mode_edit', {'id': mode.id}) }}">edit</a>

    {{ include('mode/_delete_form.html.twig') }}
{% endblock %}
